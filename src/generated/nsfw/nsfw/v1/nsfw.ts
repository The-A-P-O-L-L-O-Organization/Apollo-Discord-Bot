// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               unknown
// source: nsfw/v1/nsfw.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf";

export const protobufPackage = "nsfw.v1";

export interface AnalyzeRequest {
  /** Raw image bytes (JPEG/PNG) */
  imageData: Uint8Array;
  /** Alternative: URL to fetch (optional) */
  imageUrl: string;
  /** Classification threshold (0.0-1.0) */
  threshold: number;
  /** For logging/metrics */
  guildId: string;
  /** For logging/metrics */
  userId: string;
}

export interface AnalyzeResponse {
  isNsfw: boolean;
  /** class_name -> confidence */
  predictions: { [key: string]: number };
  maxConfidence: number;
  inferenceMs: string;
}

export interface AnalyzeResponse_PredictionsEntry {
  key: string;
  value: number;
}

export interface HealthCheckRequest {
}

export interface HealthCheckResponse {
  healthy: boolean;
  modelVersion: string;
  uptimeMs: string;
}

function createBaseAnalyzeRequest(): AnalyzeRequest {
  return { imageData: new Uint8Array(0), imageUrl: "", threshold: 0, guildId: "", userId: "" };
}

export const AnalyzeRequest: MessageFns<AnalyzeRequest> = {
  encode(message: AnalyzeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageData.length !== 0) {
      writer.uint32(10).bytes(message.imageData);
    }
    if (message.imageUrl !== "") {
      writer.uint32(18).string(message.imageUrl);
    }
    if (message.threshold !== 0) {
      writer.uint32(29).float(message.threshold);
    }
    if (message.guildId !== "") {
      writer.uint32(34).string(message.guildId);
    }
    if (message.userId !== "") {
      writer.uint32(42).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAnalyzeRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.imageData = reader.bytes();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.imageUrl = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 29) {
              break;
            }

            message.threshold = reader.float();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.guildId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.userId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AnalyzeRequest {
    return {
      imageData: isSet(object.imageData)
        ? bytesFromBase64(object.imageData)
        : isSet(object.image_data)
        ? bytesFromBase64(object.image_data)
        : new Uint8Array(0),
      imageUrl: isSet(object.imageUrl)
        ? globalThis.String(object.imageUrl)
        : isSet(object.image_url)
        ? globalThis.String(object.image_url)
        : "",
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
      guildId: isSet(object.guildId)
        ? globalThis.String(object.guildId)
        : isSet(object.guild_id)
        ? globalThis.String(object.guild_id)
        : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
    };
  },

  toJSON(message: AnalyzeRequest): unknown {
    const obj: any = {};
    if (message.imageData.length !== 0) {
      obj.imageData = base64FromBytes(message.imageData);
    }
    if (message.imageUrl !== "") {
      obj.imageUrl = message.imageUrl;
    }
    if (message.threshold !== 0) {
      obj.threshold = message.threshold;
    }
    if (message.guildId !== "") {
      obj.guildId = message.guildId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnalyzeRequest>, I>>(base?: I): AnalyzeRequest {
    return AnalyzeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnalyzeRequest>, I>>(object: I): AnalyzeRequest {
    const message = createBaseAnalyzeRequest();
    message.imageData = object.imageData ?? new Uint8Array(0);
    message.imageUrl = object.imageUrl ?? "";
    message.threshold = object.threshold ?? 0;
    message.guildId = object.guildId ?? "";
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseAnalyzeResponse(): AnalyzeResponse {
  return { isNsfw: false, predictions: {}, maxConfidence: 0, inferenceMs: "0" };
}

export const AnalyzeResponse: MessageFns<AnalyzeResponse> = {
  encode(message: AnalyzeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isNsfw !== false) {
      writer.uint32(8).bool(message.isNsfw);
    }
    globalThis.Object.entries(message.predictions).forEach(([key, value]: [string, number]) => {
      AnalyzeResponse_PredictionsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.maxConfidence !== 0) {
      writer.uint32(29).float(message.maxConfidence);
    }
    if (message.inferenceMs !== "0") {
      writer.uint32(32).int64(message.inferenceMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAnalyzeResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.isNsfw = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            const entry2 = AnalyzeResponse_PredictionsEntry.decode(reader, reader.uint32());
            if (entry2.value !== undefined) {
              message.predictions[entry2.key] = entry2.value;
            }
            continue;
          }
          case 3: {
            if (tag !== 29) {
              break;
            }

            message.maxConfidence = reader.float();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.inferenceMs = reader.int64().toString();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AnalyzeResponse {
    return {
      isNsfw: isSet(object.isNsfw)
        ? globalThis.Boolean(object.isNsfw)
        : isSet(object.is_nsfw)
        ? globalThis.Boolean(object.is_nsfw)
        : false,
      predictions: isObject(object.predictions)
        ? (globalThis.Object.entries(object.predictions) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            globalThis.Object.defineProperty(acc, key, {
              value: globalThis.Number(value),
              enumerable: true,
              configurable: true,
              writable: true,
            });
            return acc;
          },
          {},
        )
        : {},
      maxConfidence: isSet(object.maxConfidence)
        ? globalThis.Number(object.maxConfidence)
        : isSet(object.max_confidence)
        ? globalThis.Number(object.max_confidence)
        : 0,
      inferenceMs: isSet(object.inferenceMs)
        ? globalThis.String(object.inferenceMs)
        : isSet(object.inference_ms)
        ? globalThis.String(object.inference_ms)
        : "0",
    };
  },

  toJSON(message: AnalyzeResponse): unknown {
    const obj: any = {};
    if (message.isNsfw !== false) {
      obj.isNsfw = message.isNsfw;
    }
    if (message.predictions) {
      const entries = globalThis.Object.entries(message.predictions) as [string, number][];
      if (entries.length > 0) {
        obj.predictions = {};
        entries.forEach(([k, v]) => {
          obj.predictions[k] = v;
        });
      }
    }
    if (message.maxConfidence !== 0) {
      obj.maxConfidence = message.maxConfidence;
    }
    if (message.inferenceMs !== "0") {
      obj.inferenceMs = message.inferenceMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnalyzeResponse>, I>>(base?: I): AnalyzeResponse {
    return AnalyzeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnalyzeResponse>, I>>(object: I): AnalyzeResponse {
    const message = createBaseAnalyzeResponse();
    message.isNsfw = object.isNsfw ?? false;
    message.predictions = (globalThis.Object.entries(object.predictions ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.maxConfidence = object.maxConfidence ?? 0;
    message.inferenceMs = object.inferenceMs ?? "0";
    return message;
  },
};

function createBaseAnalyzeResponse_PredictionsEntry(): AnalyzeResponse_PredictionsEntry {
  return { key: "", value: 0 };
}

export const AnalyzeResponse_PredictionsEntry: MessageFns<AnalyzeResponse_PredictionsEntry> = {
  encode(message: AnalyzeResponse_PredictionsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(21).float(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeResponse_PredictionsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAnalyzeResponse_PredictionsEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 21) {
              break;
            }

            message.value = reader.float();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AnalyzeResponse_PredictionsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: AnalyzeResponse_PredictionsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnalyzeResponse_PredictionsEntry>, I>>(
    base?: I,
  ): AnalyzeResponse_PredictionsEntry {
    return AnalyzeResponse_PredictionsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnalyzeResponse_PredictionsEntry>, I>>(
    object: I,
  ): AnalyzeResponse_PredictionsEntry {
    const message = createBaseAnalyzeResponse_PredictionsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseHealthCheckRequest(): HealthCheckRequest {
  return {};
}

export const HealthCheckRequest: MessageFns<HealthCheckRequest> = {
  encode(_: HealthCheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHealthCheckRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): HealthCheckRequest {
    return {};
  },

  toJSON(_: HealthCheckRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthCheckRequest>, I>>(base?: I): HealthCheckRequest {
    return HealthCheckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheckRequest>, I>>(_: I): HealthCheckRequest {
    const message = createBaseHealthCheckRequest();
    return message;
  },
};

function createBaseHealthCheckResponse(): HealthCheckResponse {
  return { healthy: false, modelVersion: "", uptimeMs: "0" };
}

export const HealthCheckResponse: MessageFns<HealthCheckResponse> = {
  encode(message: HealthCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.healthy !== false) {
      writer.uint32(8).bool(message.healthy);
    }
    if (message.modelVersion !== "") {
      writer.uint32(18).string(message.modelVersion);
    }
    if (message.uptimeMs !== "0") {
      writer.uint32(24).int64(message.uptimeMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHealthCheckResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.healthy = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.modelVersion = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.uptimeMs = reader.int64().toString();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): HealthCheckResponse {
    return {
      healthy: isSet(object.healthy) ? globalThis.Boolean(object.healthy) : false,
      modelVersion: isSet(object.modelVersion)
        ? globalThis.String(object.modelVersion)
        : isSet(object.model_version)
        ? globalThis.String(object.model_version)
        : "",
      uptimeMs: isSet(object.uptimeMs)
        ? globalThis.String(object.uptimeMs)
        : isSet(object.uptime_ms)
        ? globalThis.String(object.uptime_ms)
        : "0",
    };
  },

  toJSON(message: HealthCheckResponse): unknown {
    const obj: any = {};
    if (message.healthy !== false) {
      obj.healthy = message.healthy;
    }
    if (message.modelVersion !== "") {
      obj.modelVersion = message.modelVersion;
    }
    if (message.uptimeMs !== "0") {
      obj.uptimeMs = message.uptimeMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthCheckResponse>, I>>(base?: I): HealthCheckResponse {
    return HealthCheckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheckResponse>, I>>(object: I): HealthCheckResponse {
    const message = createBaseHealthCheckResponse();
    message.healthy = object.healthy ?? false;
    message.modelVersion = object.modelVersion ?? "";
    message.uptimeMs = object.uptimeMs ?? "0";
    return message;
  },
};

export interface NsfwService {
  Analyze(request: AnalyzeRequest): Promise<AnalyzeResponse>;
  HealthCheck(request: HealthCheckRequest): Promise<HealthCheckResponse>;
}

export const NsfwServiceServiceName = "nsfw.v1.NsfwService";
export class NsfwServiceClientImpl implements NsfwService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || NsfwServiceServiceName;
    this.rpc = rpc;
    this.Analyze = this.Analyze.bind(this);
    this.HealthCheck = this.HealthCheck.bind(this);
  }
  Analyze(request: AnalyzeRequest): Promise<AnalyzeResponse> {
    const data = AnalyzeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Analyze", data);
    return promise.then((data) => AnalyzeResponse.decode(new BinaryReader(data)));
  }

  HealthCheck(request: HealthCheckRequest): Promise<HealthCheckResponse> {
    const data = HealthCheckRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "HealthCheck", data);
    return promise.then((data) => HealthCheckResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
